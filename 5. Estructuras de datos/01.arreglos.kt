fun main() {
    // Los arreglos son colecciones mutables de elementos con acceso aleatorio por indice
    val v1: Array<Int> = arrayOf(1,2,3,4,5)

    // Se pueden acceder a elementos especificos a traves de su indice
    println(v1[0]) // Se accede al elemento ubicado en la primera posicion del arreglo
}